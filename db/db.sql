-- creating and using the data base. 
-- Set the table info in the schema.sql
-- use cl "source db/db.sql" to create the database
-- enable the creation of a new database and only attempt to drop the database if it exists.
DROP DATABASE IF EXISTS election;
CREATE DATABASE election;
USE election;